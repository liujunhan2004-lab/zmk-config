# Ferris Sweep + urob 布局 使用教程

本教程面向已经刷好本仓库固件的 Ferris Sweep 键盘。这套布局是 urob 的
**Colemak-DH + 主行 mod + 组合键**方案,和普通 QWERTY 键盘的用法差别很大,
请耐心读完,循序渐进上手。

---

## 目录

1. [核心概念](#一核心概念先读这个)
2. [基础层 Colemak-DH](#二基础层-colemak-dh)
3. [主行 mod(HRM)](#三主行-modhrm)
4. [四个拇指键](#四四个拇指键最重要的四个键)
5. [六个层总览](#五六个层总览)
6. [组合键:输入符号](#六组合键输入符号)
7. [智能层](#七智能层numword--smart-mouse)
8. [特殊功能速查](#八特殊功能速查)
9. [蓝牙配对与设备切换](#九蓝牙配对与设备切换)
10. [刷机与复位](#十刷机与复位)
11. [上手路线建议](#十一上手路线建议)
12. [常见问题](#十二常见问题)
13. [附录:外部工具依赖](#附录外部工具依赖)

---

## 一、核心概念(先读这个)

这套布局用三个思想替代了传统键盘的"更多键":

1. **主行 mod(HRM)** — 主行字母键**轻按是字母,长按是修饰键**(Ctrl/Shift/Alt/Win),
   双手打字时不用离开主行。
2. **组合键(combo)** — **同时按下两个相邻键**输入一个符号,取代了符号层。
3. **层(layer)** — 通过**长按拇指键**进入,松手退出,取代了 F 区、数字区、方向区。

> 关键理解:这个键盘上**大多数按键都有"轻按"和"长按"两种动作**,轻按是
> 标在键帽上的内容,长按是它的"第二功能"。这是和普通键盘最大的区别。

---

## 二、基础层 Colemak-DH

默认层是 **Colemak-DH** 字母布局,**不是 QWERTY**。字母位置如下(小写为
轻按输出,大写标注的键同时是主行 mod,见下一节):

```
Q   W   F   P   B        J   L   U   Y   '
A   R   S   T   G        M   N   E   I   O
Z   X   C   D   V        K   H   ,   .   ?
     [空格/NAV] [回车/FN]    [Num]  [Magic]
```

- 与 QWERTY 相比,主要变化:`E`/`R`/`T`、`S`/`D` 等字母换了位置。
- `,` `.` `?` 三个键有 Shift 变体(见[第八节](#八特殊功能速查))。
- `'` 是单引号(在右顶行最外侧)。

---

## 三、主行 mod(HRM)

主行中间的 8 个字母键,轻按是字母,**长按是修饰键**:

| 键 | 轻按 | 长按(按住) |
|---|---|---|
| A | a | **Win / Cmd**(LGUI) |
| R | r | **Alt**(LALT) |
| S | s | **Shift**(LSHFT) |
| T | t | **Ctrl**(LCTRL) |
| N | n | **Ctrl**(LCTRL) |
| E | e | **Shift**(RSHFT) |
| I | i | **Alt**(LALT) |
| O | o | **Win / Cmd**(LGUI) |

**用法举例:**

- `Ctrl+C` = 按住 `T`(或 `N`)+ 轻按 `C`(C 在基础层左下第二个)
- `Ctrl+Shift+Tab` = 按住 `T` + `S` + 再按 Tab
- 全选 `Ctrl+A` = 按住 `T` + 轻按 `A`

**技巧:**
- 修饰键分布在**双手**,所以 `Ctrl+某字母` 时,一只手按修饰,另一只手按字母,
  比单手方便。
- 想"单独按一下 Win 键"(如 Windows 打开开始菜单):**按住 `A` 超过约 280ms**
  再松开即可。
- 不需要刻意控制快慢——这套 HRM 调成了"近乎无定时",正常打字几乎不会误触发。

---

## 四、四个拇指键(最重要的四个键)

左右各两个拇指键,是整个布局的中枢:

### 左拇指内侧 —— 空格 / Nav

| 动作 | 效果 |
|---|---|
| 轻按 | 空格 |
| 长按 | 进入 **Nav 层**(松手退出) |
| Shift + 轻按 | 句号 + 空格 + 粘滞 Shift(句末大写下一句) |

### 左拇指外侧 —— 回车 / Fn

| 动作 | 效果 |
|---|---|
| 轻按 | 回车 |
| 长按 | 进入 **Fn 层**(松手退出) |

### 右拇指内侧 —— Smart-Num

| 动作 | 效果 |
|---|---|
| 轻按 | **Numword**:进入数字层,输完数字自动退出 |
| 长按 | 临时进入 **Num 层**(松手退出) |
| 双击 | 粘滞 Num 层(可连续输数字,手动再按一次退出) |

### 右拇指外侧 —— Magic-Shift

| 动作 | 效果 |
|---|---|
| 轻按(刚打完字母后) | **重复**上一个字母 |
| 轻按(其他情况) | **粘滞 Shift**:下一个字母大写 |
| 长按 | 普通 **Shift** |
| 双击 | **Caps Word**:整个词大写,词结束自动关闭 |

> 大拇指的任务最重,建议优先熟悉这四个键的"轻按 vs 长按"。

---

## 五、六个层总览

| 层 | 进入方式 | 内容 |
|---|---|---|
| **Base** | 默认 | Colemak-DH 字母 |
| **Nav** | 长按左拇指内(空格) | 方向键、翻页、窗口切换 |
| **Fn** | 长按左拇指外(回车) | F1–F12、媒体、桌面 |
| **Num** | 长按 Smart-Num | 数字键盘(左半) |
| **Sys** | 先按住 Num,再按住 Fn | 蓝牙、USB/蓝牙切换、刷机、复位 |
| **Mouse** | 组合键 W+P | 鼠标移动/滚轮/按键(右半) |

### Nav 层(长按空格)

```
F4     —      ⇧Tab  切换器  —        PgUp   ⌫词     ↑     Del词   —
Win    Alt    Shift  Ctrl   —        PgDn   ←       ↓      →     ⏎
—      —      —      —      —        Ins    Tab     —      —     —
                —      —              —     CANCEL
```

- **方向键**在右半:↑↓←→(对应 `NAV_UP/DOWN/LEFT/RIGHT`)
  - **长按**方向键:`↑`=文档开头、`↓`=文档结尾、`←`=行首、`→`=行尾
- `⌫词`(`NAV_BSPC`):轻按退格,**长按删除前一个词**
- `Del词`(`NAV_DEL`):轻按删除,**长按删除后一个词**
- `PgUp` / `PgDn`:翻页
- 左半 `F4` = Alt+F4 关窗口;`⇧Tab` = Shift+Tab
- `切换器`(`swapper`)= 一只手 Alt-Tab:轻按在窗口间切换,长按反向
- 左半的 `Win/Alt/Shift/Ctrl` = 粘滞修饰键
- `CANCEL` = 关闭所有智能层(Numword / Caps Word / Smart-mouse)

### Fn 层(长按回车)

```
F12   F7    F8    F9    —        —     ⏮      音+     ⏭     —
F11   F4    F5    F6    —        —     前桌面  音-   后桌面  —
F10   F1    F2    F3    —        固定A  固定W  桌面    —     —
                —      —              静音   播放
```

- **F1–F12** 按数字键盘的镜像排布在左半:
  - 顶行 `F12 F7 F8 F9`,中行 `F11 F4 F5 F6`,底行 `F10 F1 F2 F3`
- **媒体键**(右半):⏮ 上一首、⏭ 下一首、音+/音-、静音、播放/暂停
- **桌面**(右半):前桌面、后桌面、固定窗口到所有桌面、固定应用到所有桌面、桌面总览
  > ⚠️ 这 5 个键需配合 [win-11-virtual-desktop-enhancer](https://github.com/urob/win-11-virtual-desktop-enhancer) 工具,否则无效(见[附录](#附录外部工具依赖))。

### Num 层(长按 Smart-Num)

```
—      7      8      9      —        —   —   —   —   —
0      4      5      6      —        —   —   —   —   —
—      1      2      3      —        —   —   —   —   —
                —      —              —   —
```

数字键盘在**左半**,镜像普通小键盘:
- `7 8 9` 顶行、`4 5 6` 中行、`1 2 3` 底行、`0` 在最内侧

### Sys 层(先按住 Num,再按住 Fn)

```
BT1    BT2    BT3    BT4    清       USB/BLE —   —   —   —
—      —      —      —     BOOT      BOOT    —   —   —   —
—      —      —      —     RST       RST     —   —   —   —
                —      —              —   —
```

- `BT1`–`BT4` = 切换到蓝牙 profile 0–3
- `清`(`BT_CLR`)= 清除当前 profile 的配对
- `USB/BLE` = 一键切换有线(USB)/蓝牙输出(位于右半顶行最左的 J 键)
- `BOOT` = 进入 bootloader(刷机)
- `RST` = 复位键盘

### Mouse 层(组合键 W+P 触发)

```
—   —   —   —   —        —    PgUp   ↑鼠标  PgDn   —
—   —   —   —   —        左滚  ←鼠标  ↓鼠标  →鼠标  右滚
—   —   —   —   —        —    左键   中键   右键    —
                —   —              上滚   下滚
```

- 右半中间 = 鼠标移动,拇指 = 滚动,底行 = 左/中/右键
- **按任意其他键自动退出**鼠标层

---

## 六、组合键:输入符号

**没有符号层**。所有符号通过**同时按下两个(相邻)键**输入。组合键分三类:

### 垂直组合键(上下相邻)

**左半:**

| 组合 | 符号 | 组合 | 符号 |
|---|---|---|---|
| W+R | `@` | R+X | `` ` `` |
| F+S | `#` | S+C | `\` |
| P+T | `$` | T+D | `=` |
| B+G | `%` | G+V | `~` |

**右半:**

| 组合 | 符号 | 组合 | 符号 |
|---|---|---|---|
| J+M | `^` | M+K | `_` |
| L+N | `+` | N+H | `-` |
| U+E | `*` | E+, | `/` |
| Y+I | `&` | I+. | `\|` |

### 水平组合键(左右相邻)

| 组合 | 效果 | 说明 |
|---|---|---|
| W+F | `Esc` | |
| F+P | 触发鼠标层 | |
| R+S | `Tab` | |
| S+T | 全选 `Ctrl+A` | |
| R+S+T | 任务管理器 `Ctrl+Shift+Esc` | 三个键一起 |
| X+C | 剪切 | |
| X+D | 复制 | |
| C+D | 粘贴 | |
| L+U | 退格 | |
| U+Y | 删除 | |
| N+E | `(` | Nav 层内为 `<` |
| E+I | `)` | Nav 层内为 `>` |
| H+, | `[` | Nav 层内为 `{` |
| ,+. | `]` | Nav 层内为 `}` |

> 记住规律:上下相邻的两键是"符号行"(和数字行符号一一对应),左右相邻
> 的两键是常用操作(复制/粘贴/括号等)。这是刻意设计的对称,便于记忆。

---

## 七、智能层(Numword / Smart-mouse)

两个"自动关闭"的层,不用手动退出:

- **Numword**:轻按 Smart-Num 进入,连续输数字保持激活,**按任何非数字键自动退出**。
  输单个数字相当于粘滞层,输多个数字也方便。
- **Smart-mouse**:组合键 `W+P` 进入,**按任意其他键自动退出**。

如果自动退出没生效(比如数字后面紧跟数字区上的字母),用 Nav 层的 `CANCEL` 键
强制关闭所有智能层。

---

## 八、特殊功能速查

### 标点 Shift 变体

| 键 | 轻按 | Shift + 轻按 | Ctrl+Shift + 轻按 |
|---|---|---|---|
| `,` | , | ; | < |
| `.` | . | : | > |
| `?` | ? | ! | — |
| `(`(N+E) | ( | < | — |
| `)`(E+I) | ) | > | — |

### 退格键(删除)

- 组合键 `L+U` = 退格(基础层)
- Nav 层 `⌫词` = 退格,长按删前一个词

### 剪贴板

- `X+C` = 剪切、`X+D` = 复制、`C+D` = 粘贴(左半水平组合键)

### 窗口与桌面

- `Alt+F4` = 关窗口(Nav 层左顶)
- `切换器` = Alt-Tab(Nav 层)
- 桌面切换/固定/总览在 Fn 层右半

---

## 九、蓝牙配对与设备切换

**分体结构**:左半是中心(central,连电脑),右半是周边(peripheral,自动连左半)。
**电脑只和左半配对**,右半无需单独配对。

### 首次配对

1. 打开左右两半电源(左半先开)
2. 左半开机后若当前 profile 无配对,会自动进入可发现状态(指示灯会闪)
3. 在电脑蓝牙设置里搜索并配对
4. 配对成功后,右半会自动连上左半

### 切换设备(最多 4 个 profile)

1. 进入 Sys 层:先按住 Smart-Num,再按住回车
2. 按 `BT1`–`BT4` 切换到对应 profile
3. 每个 profile 可配对一台不同设备

### 清除配对

- Sys 层按 `清`(`BT_CLR`)清除**当前 profile** 的配对
- 或刷 `settings_reset` 固件,清除**全部**配对

---

## 十、刷机与复位

### 进入 bootloader(刷机)

任选其一:
- **软件方式**:Sys 层按 `BOOT` 键
- **硬件方式**:数据线连电脑,**双击**板子背面的 reset 键

进入后电脑会弹出 `NICENANO` 盘,把 `.uf2` 固件拖进去即可,自动重启。

### 复位

- Sys 层按 `RST`

---

## 十一、上手路线建议

不要试图一天全掌握。建议分阶段:

1. **第 1–3 天**:只练 Colemak-DH 字母打字(可以只关掉其他功能先熟悉字母位置)。
   目标是字母位置形成肌肉记忆。
2. **第 3–7 天**:练主行 mod。重点练 `Ctrl+C/V`、`Shift` 大写、`Alt+Tab`。
   记住:按住=修饰,轻按=字母。
3. **第 2 周**:练拇指键。空格/回车/Num/Magic 的轻按与长按。这是分层的关键。
4. **第 3 周**:练组合键符号。先记几个常用的(`@` `#` `$` `%` `_` `-` `/`),
   再逐步扩展。
5. **之后**:练鼠标层(按需)。

> 建议对照 `draw/base.svg`(分层键位图)随时查看。改键请编辑
> `config/base.keymap`(层)、`config/combos.dtsi`(组合键),改完推到 GitHub
> 重新构建刷机。

---

## 十二、常见问题

**Q:打出字母而不是想要的大写/修饰了?**
主行 mod 的判定是"按住=修饰,轻按=字母"。按住时长超过约 280ms 才算修饰。
如果是想单独按 Win/Ctrl 等,多按一会儿。

**Q:符号打不出来?**
符号都在组合键上,需要**两个相邻键同时按下**。参考[第六节](#六组合键输入符号)
的表。没有独立的符号层。

**Q:数字怎么打?**
轻按 Smart-Num 进入 Numword,或长按进入 Num 层,数字键盘在左半。

**Q:方向键在哪?**
长按空格进入 Nav 层,右半是方向键。

**Q:想输入大写词?**
双击右拇指外侧(Magic-Shift)触发 Caps Word,或按住它当普通 Shift。

**Q:键盘没反应 / 连不上?**
检查电源、检查是否配对成功(见[第九节](#九蓝牙配对与设备切换))。
必要时 Sys 层 `清` 清除配对后重新配对。

**Q:想改键?**
看 `AGENTS.md`(改动指引)和 `README.md`。改完推到 GitHub 重新构建刷机。

---

## 附录:外部工具依赖

本布局**绝大部分功能都内置在固件里**,只有下面一个功能依赖 Windows 上的外部软件,
只需**下载安装运行**,键盘配置无需任何改动。

### win-11-virtual-desktop-enhancer —— 桌面管理键

Fn 层的 5 个桌面键(PDesk/NDesk/PinW/PinA/DSK_MGR)依赖它。

- 仓库:<https://github.com/urob/win-11-virtual-desktop-enhancer>
- 安装:Code → Download ZIP 解压,双击 `virtual-desktop-enhancer.exe` 运行(已带预编译 exe,无需装 AutoHotkey)
- 托盘出现图标即运行成功;需保持 exe 和 `settings.ini` 同目录
- 开机自启(可选):`Win+R` → `shell:startup` → 放入 exe 的快捷方式

不装它,这 5 个键按下无效果(其余功能不受影响)。
